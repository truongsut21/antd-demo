version: '3'
services:
  react-app:
    build: .
    ports:
      - "3000:3004"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm start
